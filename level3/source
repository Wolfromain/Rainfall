#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int target;
int stdin;
int stdout;


void v()
{
    char buf[512];

    fgets(buf, 512, stdin);
    printf(buf)
    if (64 == target)
    {
        fwrite("Wait what?!\n", 12, stdout);
        system("/bin/sh");
    }
    return();

}

int main(void)
{
    v();
    return (0);
}