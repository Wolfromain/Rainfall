#include <stdio.h>
#include <string.h>

int run(void)
{
    fwrite("Good... Wait what?\n", 19, 1, stdout);
    return (system("/bin/sh"));
}

int main(void)
{
    char buf[0x40];

    gets(buf);
    return (0);
}