#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int     n(void)
{
    char    buffer[512];

    fgets(buffer, 512, stdin);
    p(buffer);
    if (? == 16930116)
        system("/bin/cat /home/user/level5/.pass");
    return (0);
}

int p(char *buffer)
{
    printf(buffer);
    return(0);
}

int main(void)
{
    n();
    return (0);
}