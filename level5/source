#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int stdin;

int     n(void)
{
    char    buffer[512];

    fgets(buffer, 512, stdin);
    printf(buffer);
    exit(1);
}

int o(char *buffer)
{
    system("/bin/sh");
    exit(1);
}

int main(void)
{
    n();
    return (0);
}