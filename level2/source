#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void p()
{
    char buf[64];
    int

    fflush(stdout);
    gets(buf);
    if((buf & 2952790016) == 2952790016)
    {
        printf("(%p)\n", buf);
        exit(1);
    }
    puts(buf)
    strdup(buf);
}

int main(void)
{
    p();
    return (0);
}