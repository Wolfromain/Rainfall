#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void p(char *arg)
{
    char buf[64];

    fflush(stdout);
    gets(buf);
    if((arg & 2952790016) == 2952790016)
    {
        printf("(%p)\n", arg);
        exit(1);
    }
    puts(buf)
    strdup(buf);
}

int main(void)
{
    p();
    return (0);
}